{{--
 <footer class="py-2 mt-auto serveit-footer">
  <div class="container-fluid px-4">
    <div class="d-flex align-items-center justify-content-between small">
      <div class="text-muted">Copyright &copy; ServeIT 2025</div>
      <div>
        <a href="#">Privacy Policy</a>
        &middot;
        <a href="#">Terms &amp; Conditions</a>
      </div>
    </div>
  </div>
</footer>

<style>
  :root {
    --brand-500:#3b82f6; 
    --brand-600:#2563eb; 
    --brand-700:#1d4ed8;
    --text:#e5e7eb; 
    --muted:#94a3b8; 
    --border:#1f2a44;
  }

  /* Compact techy footer */
  footer.serveit-footer {
    position: relative;
    background: linear-gradient(180deg, rgba(15,23,42,.95), rgba(11,18,32,.95));
    border-top: 1px solid var(--border);
    color: var(--muted);
    padding-top: .4rem !important;     /* reduced from 1rem */
    padding-bottom: .4rem !important;  /* reduced from 1rem */
    font-size: 0.8rem;                 /* slightly smaller text */
  }

  /* thin neon strip at top of footer */
  footer.serveit-footer::before {
    content: "";
    position: absolute;
    left: 0; right: 0; top: 0; height: 2px;
    background: linear-gradient(90deg, var(--brand-600), var(--brand-700));
    opacity: .9;
  }

  footer.serveit-footer .text-muted {
    color: var(--muted) !important;
  }

  footer.serveit-footer a {
    color: var(--text);
    text-decoration: none;
    opacity: .85;
    transition: color .15s ease, opacity .15s ease;
  }
  footer.serveit-footer a:hover {
    color: #cfe1ff;
    opacity: 1;
  }

  /* Mobile stacking */
  @media (max-width: 576px) {
    footer.serveit-footer .d-flex {
      flex-direction: column !important;
      gap: .25rem;
      text-align: center;
    }
  }
</style> 

--}}